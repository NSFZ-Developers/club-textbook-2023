#include <bits/stdc++.h> 
template <typename T>
auto _max = std::numeric_limits<T>::max();
template <typename T>
auto _min = std::numeric_limits<T>::min();
#define out(x) std::cout << #x << ": " << (x) << std::endl;
#define out_max(t) out(_max<t>);
#define out_min(t) out(_min<t>);
int main() {
	
	out_max(float)
	out_min(float)
	
	out_max(double)
	out_min(double)
	
	out_max(long double)
	out_min(long double)
	
	return 0;
}
